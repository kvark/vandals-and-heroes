(
    map: "fostral",
    car: "OxidizeMonk",
    environment: Some("Fostral"),
    // Debug-snow density: one particle per N m² of world surface area.
    // Smaller = denser snow = slower frame. 0.0 keeps the default (~2.5).
    snow_area_per_particle_m2: 2.5,
    ray: (
        march_count: 50,
        march_closest_power: 2,
        bisect_count: 20,
    ),
    record: Some((
        path: "state.log.ron",
        format: Ron,
    )),
)
